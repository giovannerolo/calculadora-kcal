export default class Calculadora {
  constructor(calculadora, kcal) {
    this.calculadora = document.querySelector(calculadora)
    this.kcal = document.querySelectorAll(kcal);
  }
  incrementar() {
    const total = this.cal.innertext;
    
  }
}